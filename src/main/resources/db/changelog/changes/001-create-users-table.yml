databaseChangeLog:
  - changeSet:
      author: Tim
      id: 1
      changes:
        - createTable:
            tableName: users
            columns:
              - column:
                  name: id
                  type: bigint
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: username
                  type: varchar(100)
                  constraints:
                    nullable: false
                    unique: true
              - column:
                  name: password
                  type: varchar(100)
                  constraints:
                    nullable: false
                    unique: false

        - sql:
            sql: "ALTER TABLE users ADD CONSTRAINT chk_username_min_length CHECK (LENGTH(username) >= 3);"
        - sql:
            sql: "ALTER TABLE users ADD CONSTRAINT chk_password_min_length CHECK (LENGTH(password) >= 3);"
